### **《时空回响》数据格式说明书 v1.3**



这份文档是在《时空回响》世界中创作冒险、谱写故事的蓝图。它将详细解释如何通过配置简单的 `.json` 文件，来创建地图、放置物品、设计对话和实现复杂的剧情逻辑。

相较于 v1.2 版本，v1.3 新增和优化了以下重要功能，能让故事更加生动和强大：

- **入场动画**: 可以在故事开始前播放一段视频动画，增强沉浸感。
- **统一的剧情节点动作 (`action`)**: 现在，任何故事节点（包括入场剧情）都可以触发传送、启动小游戏、存档等关键行为，极大地丰富了剧情表现力。
- **灵活的数值修改**: 增加了可以直接设置数值的 `absolute` 字段，而不仅仅是增减。
- **新增数据类型**: 增加了对**成就 (`achievements`)**、**手动存档介绍 (`savefile_intro`)** 和 **初始数值定义 (`value_initialize.json`)** 的支持。

------



这里是历史版本的更新信息：

相较于 v1.1 版本，v1.2 新增和优化了以下重要功能：

- **统一数值检查的字段**: 统一数值检查的字段名称，对剧情创作者更友好。
- **存档行为**: 配合游戏的存/读档系统，在故事中设置存档点。
- **统一入场剧情行为**: 在旧版中，入场剧情无法触发传送/加载小游戏等行为，现在入场剧情与基础剧情无异。
- **调整传送数据存放位置**: 新版支持把 `teleportData` 放置在故事json文件里面，进而支持同一个传送点可以传送至不同地图，仍兼容老版本的写法。

相较于 v1.0 版本，v1.1 新增和优化了以下重要功能：

- **入场剧情**: 现在可以为地图设置一个“入场故事”，当玩家第一次进入地图时，会自动播放一段剧情。
- **一次性互动**: 可以轻松地将某个物品设置为“一次性的”，玩家与它互动一次后，它就会从游戏中消失。
- **更强大的显示逻辑**: 物体的出现条件得到了增强，现在支持多个条件之间的“与”(AND) / “或”(OR) 逻辑，可以设计出更复杂的隐藏要素。
- **地图尺寸设置**: 设定地图尺寸以实现镜头视角随玩家移动，在地图边界上会自动添加墙体。

v1.0版本（初始版本）支持的功能：

- **物品信息**: 物品的大小、坐标、图片、交互故事和传送信息等。
- **地图信息**: 地图的背景图片、物品列表等。
- **故事信息**: 故事的文本、人物、人物立绘等。

----



#### **一、 核心数据文件概览**



我们整个游戏的核心都围绕着 `.json` 文件。它们定义了游戏世界的一切。

主要有以下几类核心数据：

1. **地图 (`/data/maps`)**：定义一个游戏场景的宏观数据，比如背景、墙体、尺寸和其中包含的物体。
2. **物体 (`/data/objects`)**：定义了可以放置在地图上的具体实体，比如一个NPC、一个传送门或一个满足特定条件才会出现的隐藏物品。
3. **故事 (`/data/stories`)**：定义了与物体交互或进入地图时触发的对话、剧情分支和游戏数值变化。
4. **成就 (`/data/achievements`)**：定义了游戏中的各项成就及其解锁条件。
5. **存档介绍 (`/data/savefile_intro`)**: 为手动存档点提供在读档界面显示的元数据。
6. **全局配置 (`/data/*.json`)**:
   - `value_initialize.json`: 定义所有玩家数值的初始值和读档时不可覆盖的数值列表。
   - `tutorial.json`: 定义新手引导的内容。

------



#### **二、 地图文件 (`/data/maps/*.json`)**



地图文件负责“画”出整个游戏场景。它像一个大“舞台”，告诉程序这个场景叫什么、长什么样，以及上面要“摆放”哪些“道具”（物体）。

目录位置: /data/maps/

命名约定: 你的地图名.json (例如: map1.json)

**字段说明：**

| 字段名          | 类型   | 是否必需 | 描述                                                         |
| --------------- | ------ | -------- | ------------------------------------------------------------ |
| `name`          | String | 是       | 地图的名称。比如 "起源之地"。                                |
| `background`    | String | 是       | 指向地图背景图片的路径。例如 `"assets/background/background1-1.png"`。 |
| `width`         | Number | 否       | 地图的宽度（单位：像素）。默认为 `800`。当地图远大于玩家视口时，必须设置此项以实现镜头滚动。 |
| `height`        | Number | 否       | 地图的高度（单位：像素）。默认为 `600`。                     |
| `music`         | String | 否       | 指向本地图背景音乐的路径。例如 `"assets/music/Anthurium.mp3"`。 |
| `walls`         | Array  | 否       | 一个包含多个“墙体”对象的数组。用于设置碰撞体积，防止玩家穿墙。 |
| `objects`       | Array  | 否       | 一个**字符串数组**，包含了所有要在这个地图上**初始加载**的物体的 **文件名（不带.json后缀）**。 |
| `entryStoryKey` | String | 否       | 玩家进入该地图时自动触发的**故事文件名（不带.json后缀）**。非常适合用于制作开场动画或区域介绍。 |

**`walls` 内部对象结构：**

| 字段名   | 类型   | 描述                |
| -------- | ------ | ------------------- |
| `x`      | Number | 墙体左上角的X坐标。 |
| `y`      | Number | 墙体左上角的Y坐标。 |
| `width`  | Number | 墙体的宽度。        |
| `height` | Number | 墙体的高度。        |

示例 (Map2-2.json):

这个例子展示了一个典型的地图定义，包含了音乐和入场剧情。

```json
{
  "name": "街心花园",
  "width": 1600,
  "height": 900,
  "background": "assets/background/background2-2.png",
  "music": "assets/music/Slient Love.mp3",
  "entryStoryKey": "Map2-2_entry_story",
  "walls": [
    {"x": 570, "y": 0, "width": 450, "height": 350},
    {"x": 650, "y": 500, "width": 300, "height": 200}
  ],
  "objects": [ "FlowerShop2-2","XiaoXing2-2"]
}
```

------



#### **三、 物体文件 (`/data/objects/\*.json`)**



物体是地图上的核心交互元素。它可以是一个能对话的NPC、一个能捡的道具、一个传送门，甚至是一个满足特定条件才会出现的隐藏物品。

目录位置: /data/objects/

命名约定: 你的物体名.json (例如: portal1to2.json)

**字段说明：**

| 字段名                      | 类型    | 是否必需 | 描述                                                         |
| --------------------------- | ------- | -------- | ------------------------------------------------------------ |
| `id`                        | String  | 是       | 物体的唯一标识符，**必须和文件名保持一致**。例如 `"portal1to2"`。 |
| `image`                     | String  | 是       | 指向该物体图片的路径。如果为空字符串`""`，则物体在地图上不可见，但仍可交互。 |
| `x`, `y`, `width`, `height` | Number  | 是       | 物体在地图上的位置和尺寸。                                   |
| `storyKey`                  | String  | 是       | 关联的**故事文件名（不带.json后缀）**。当玩家与此物体交互时，会加载并开始这个故事。 |
| `showPrompt`                | Boolean | 否       | 是否在玩家靠近时显示交互提示（'E'键）。默认为 `true`。       |
| `singleInteraction`         | Boolean | 否       | 如果设为 `true`，玩家与该物体交互并**完成关联故事后**，该物体将**永久消失**。默认为 `true`。 |
| `teleportData`              | Object  | 否       | 如果这个物体是传送门，需要此字段定义传送目标。**注意：此功能也可在故事文件中实现，以获得更强的灵活性**。 |
| `requiredValues`            | Object  | 否       | 物体**出现**的条件。如果玩家的数值不满足这些条件，这个物体根本不会出现在地图上。 |

**`teleportData` 对象结构：**

| 字段名               | 类型   | 描述                                    |
| -------------------- | ------ | --------------------------------------- |
| `targetMap`          | String | 目标地图的**文件名（不带.json后缀）**。 |
| `targetX`, `targetY` | Number | 玩家传送到目标地图后的坐标。            |

**`requiredValues` 对象结构:**

| 字段名       | 类型   | 描述                                                         |
| ------------ | ------ | ------------------------------------------------------------ |
| `logic`      | String | 条件的组合逻辑。`"AND"` (默认)表示所有条件都必须满足；`"OR"` 表示只需满足任意一个条件。 |
| `conditions` | Array  | 一个包含所有具体条件的数组。                                 |

**`conditions` 数组内的对象结构:**

| 字段名       | 类型   | 描述                                                         |
| ------------ | ------ | ------------------------------------------------------------ |
| `name`       | String | 需要检查的玩家数值的名称，例如 `"suspicion"`。               |
| `comparison` | String | 比较方式。必须是以下之一：`"greater_than"`, `"less_than"`, `"equal"`, `"greater_than_or_equal"`, `"less_than_or_equal"`, `"not_equal"`。 |
| `value`      | Number | 用于比较的阈值。                                             |

示例 (test.json):

这个物品需要同时满足两个条件才会出现（怀疑度大于等于150 并且 海螺值为0）。

```json
{
  "id": "test",
  "image": "assets/images/item_01.png",
  "x": 800, "y": 750, "width": 50, "height": 50,
  "storyKey": "high_suspicion_story",
  "singleInteraction": false,
  "showPrompt": true,
  "requiredValues": {
    "logic": "AND",
    "conditions": [
      {"name": "suspicion", "comparison": "greater_than_or_equal", "value": 150},
      {"name": "conch", "comparison": "equal", "value": 0}
    ]
  }
}
```

------



#### **四、 故事文件 (`/data/stories/*.json`)**



故事是整个游戏最有趣的部分！它是一个由多个“节点”组成的对话树，通过对话、选项和各种动作，来驱动剧情发展。

目录位置: /data/stories/

命名约定: 你的故事名.json (例如: portal_story.json)

**顶层字段说明：**

| 字段名      | 类型   | 是否必需 | 描述                                                         |
| ----------- | ------ | -------- | ------------------------------------------------------------ |
| `startNode` | String | 是       | 故事开始的节点ID。例如 `"node1"`。                           |
| `animation` | String | 否       | **(v1.3 新增)** 指向在故事开始前播放的视频动画路径。例如 `"assets/animation/01.mp4"`。 |
| `nodes`     | Object | 是       | 包含所有故事节点的“字典”，`key` 是节点ID，`value` 是节点对象。 |

**`nodes` 内部的节点对象结构：**

| 字段名         | 类型   | 是否必需 | 描述                                                         |
| -------------- | ------ | -------- | ------------------------------------------------------------ |
| `speaker`      | String | 是       | 对话者的名字。如果是 `"旁白"`，则不显示名字。                |
| `dialogue`     | String | 是       | 当前节点显示的对话文本。                                     |
| `scene`        | Array  | 否       | 定义当前场景需要显示哪些角色立绘及其图片路径。               |
| `displayImage` | String | 否       | 在对话背景中额外显示一张图片，常用于展示关键物品或场景特写。 |
| `nextNode`     | String | 否       | 如果该节点是线性对话（没有选项），此字段指向下一个节点的ID。 |
| `options`      | Array  | 否       | 如果该节点提供选项，则此字段为一个选项对象数组。**`nextNode` 和 `options` 通常不同时存在**。 |
| `valueChanges` | Array  | 否       | 一个包含数值变化对象的数组，用于改变玩家的属性。             |
| `action`       | Object | 否       | **(v1.3 增强)** 在节点结束时触发的特殊动作对象。             |

**`options` 内部的选项对象结构：**

| 字段名          | 类型   | 是否必需 | 描述                                                         |
| --------------- | ------ | -------- | ------------------------------------------------------------ |
| `text`          | String | 是       | 选项按钮上显示的文字。                                       |
| `targetNode`    | String | 是       | 点击该选项后要跳转到的节点ID。                               |
| `requiredValue` | Object | 否       | 控制**选项是否显示**的条件。结构与物体的 `conditions` 对象相同。若不存在则默认显示该选项。 |

**`valueChanges` 内部的数值变化对象结构：**

| 字段名     | 类型   | 是否必需 | 描述                                                         |
| ---------- | ------ | -------- | ------------------------------------------------------------ |
| `name`     | String | 是       | 要改变的数值名称，例如 `"suspicion"`。                       |
| `amount`   | Number | 是       | 改变的量。正数表示增加，负数表示减少。例如 `100` 或 `-50`。  |
| `absolute` | Number | 否       | **(v1.3 新增)** 如果此字段存在 (值为任意数字均可)，`amount` 将直接**覆盖**原数值，而不是增减。例如，`"name": "reachEnd", "amount": 1, "absolute": 1` 会将`reachEnd`的值直接设为`1`。 |

**`action` 内部的动作对象结构：**

| 字段名         | 类型   | 描述                                                         |
| -------------- | ------ | ------------------------------------------------------------ |
| `type`         | String | 动作的类型。                                                 |
| `onWin`        | String | **(小游戏专属)** 小游戏胜利后跳转的故事文件名。              |
| `onLose`       | String | **(小游戏专属)** 小游戏失败后跳转的故事文件名。              |
| `saveFileName` | String | **(存档专属)** 要创建的手动存档的文件名。                    |
| `teleportData` | Object | **(传送专属)** 定义传送目标的 `teleportData` 对象，结构同物体文件。 |

**`action.type` 的可用值：**

- `"teleport"`: 触发传送。可配合节点内的`teleportData`或物体上的`teleportData`使用。
- `"saveFile"`: 创建一个手动存档。
- `"FTG"`, `"Parkour"`, `"STG"`, `"Mario"`, `"Rope"`, `"jiemi"`: 启动对应的小游戏。

示例 (Trainer2-4-2_story.json):

这个故事展示了如何触发一个小游戏，并根据胜负结果跳转到不同的后续故事。

```json
{
  "startNode": "node1",
  "nodes": {
    "node1": {
      "scene": [
        { "id": "唐梓", "sprite": "assets/images/charactertz.png" },
        { "id": "管理员", "sprite": "assets/images/Trainer2.png" }
      ],
      "speaker": "唐梓",
      "dialogue": "您好，请问这里是体能训练基地吗？",
      "nextNode": "node2"
    },
    "node2": {
      "scene": [
        { "id": "唐梓", "sprite": "assets/images/charactertz.png" },
        { "id": "管理员", "sprite": "assets/images/Trainer2.png" }
      ],
      "speaker": "管理员",
      "dialogue": "同志您好，您是今天来结业考核的唐梓同志吧！欢迎来到体能训练基地！",
      "action":{
        "type":"Parkour",
        "onWin": "Parkour_win_story",
        "onLose": "Parkour_lose_story"
      }
    }
  }
}
```

------



#### **五、 成就文件 (`/data/achievements/*.json`)**



成就文件定义了游戏中的一项成就，包括它的显示信息和解锁条件。

目录位置: /data/achievements/

命名约定: 你的成就名.json (例如: `memoryAlpha.json`)

**字段说明：**

| 字段名                   | 类型   | 是否必需 | 描述                                                         |
| ------------------------ | ------ | -------- | ------------------------------------------------------------ |
| `id`                     | Number | 是       | 成就的数字ID，用于排序。                                     |
| `type`                   | String | 是       | 成就的分类，如 `memory`, `character`, `mission`, `decrypt`。 |
| `name`                   | String | 是       | 成就的名称，例如 "记忆碎片Alpha：海螺头绳"。                 |
| `abstract`               | String | 是       | 成就的简短摘要或提示语。                                     |
| `descriptionUncompleted` | String | 是       | 成就**未解锁时**显示的描述。                                 |
| `descriptionCompleted`   | String | 是       | 成就**已解锁后**显示的详细描述。                             |
| `icon`                   | String | 是       | 代表该成就的Emoji图标或字符。                                |
| `requiredValues`         | Object | 是       | 解锁该成就的条件，结构与物体文件的 `requiredValues` 完全相同。 |

**示例 (`memoryAlpha.json`):**

```json
{
  "id": 1,
  "type": "memory",
  "name": "记忆碎片Alpha：海螺头绳",
  "abstract": "阿巴阿巴",
  "descriptionUncompleted": "“任务结束后，要来我的家乡哦！我带你去海边玩！”",
  "descriptionCompleted": "“任务结束后，要来我的家乡哦！我带你去海边玩！”...",
  "icon": "\uD83E\uDDE3",
  "requiredValues": {
    "conditions": [
      {"name": "tousheng", "comparison": "greater_than_or_equal", "value": 1}
    ]
  }
}
```

------



#### **六、 其他关键数据文件**



1. 初始数值定义 (`/data/value_initialize.json`)

   该文件是整个数值系统的基础，定义了新玩家创建时的默认数值，以及在读档时哪些数值不应被旧存档覆盖。

   - `values`: 一个对象，定义了所有游戏内变量的初始值。
   - `valuesNoOverwrite`: 一个字符串数组，列出了在加载手动存档时，需要保留玩家当前主存档数值的变量名（例如，总游戏时长、已解锁的记忆碎片等）。

2. 手动存档介绍 (`data/savefile_intro/*.json`)

   这些文件为手动存档点提供了在读档界面显示的静态信息。文件名需要与故事中 `saveFile` 动作指定的 `saveFileName` 对应。

   - `id`: 存档点的数字ID，用于排序。
   - `fileName`: 在读档界面显示的存档名称。
   - `location`: 存档时所在的地点。
   - `description`: 对该存档点的简短描述。
   - `progress`: 表示主线进度的百分比。
