### **《时空回响》技术方案说明**





#### **引言**



本项目是一款基于Web技术的2D叙事驱动冒险游戏。玩家在游戏中扮演主角，通过在不同场景中探索、与NPC对话、完成迷你游戏来推动剧情发展。项目采用前后端分离的现代Web架构，后端由高性能的Rust语言和Actix Web框架构建，负责处理游戏逻辑、数据持久化和用户认证；前端则采用原生JavaScript（ES6 Modules）构建，负责游戏画面的渲染、用户交互和与后端的通信。本文档旨在详细阐述项目所采用的技术方案、系统设计和关键实现细节。

------



#### **一、 技术选型**



为了实现一个高性能、可维护且具有良好交互体验的Web游戏，我们选择了以下技术栈：

1. **后端 (Backend):**
   - **语言与框架:** **Rust + Actix Web**。选择Rust是因为其卓越的性能、内存安全和并发能力，这对于需要处理实时数据交互和潜在多用户场景的游戏服务器至关重要。Actix Web是一个高性能、功能强大的异步Web框架，其Actor模型非常适合构建响应迅速的API服务。
   - **数据持久化:** **JSON文件系统**。在项目的当前阶段，我们选择将用户数据、游戏存档、地图定义等信息以JSON文件的形式存储在服务器磁盘上。这种方式轻量、直观，便于开发和调试，并且对于项目当前的用户体量来说，性能完全足够。
   - **认证机制:** **JSON Web Tokens (JWT)**。我们使用JWT进行无状态的用户身份验证。用户登录成功后，服务器会签发一个包含用户信息的Token，前端将其存储起来。后续所有需要认证的API请求都会在请求头中携带此Token，服务器验证其有效性后即可识别用户身份，无需依赖Session。
2. **前端 (Frontend):**
   - **核心技术:** **原生JavaScript (ES6 Modules) + HTML5 + CSS3**。我们没有选择React或Vue等重型框架，而是回归本源，使用原生JS。这使得我们能够更精细地控制DOM、优化性能，并构建一个轻量级的、专为游戏逻辑服务的客户端。ES6模块化的使用（`import`/`export`）保证了代码的组织性、可读性和可维护性。
   - **代码结构:** 采用高度模块化的结构，将游戏逻辑拆分为数据管理、音频管理、世界构建、碰撞检测、交互逻辑、对话系统等多个独立的模块，实现了高度的解耦和复用。
3. **前后端通信:**
   - **协议与格式:** 基于**HTTP/S的RESTful API**。所有前后端的数据交换都通过定义清晰的API端点进行，数据格式统一为**JSON**，这是一种轻量且易于解析的通用格式。前端使用浏览器原生的`fetch` API并通过`async/await`语法糖来处理所有异步请求，使代码逻辑清晰。

------



#### **二、 项目文件结构**



清晰的文件结构是项目可维护性的基石。本项目前后端都遵循了“关注点分离”的原则。

1. **后端文件结构 (位于 `src/` 目录下):**
   - `main.rs`: **服务器主入口**。负责初始化Actix Web服务器，配置监听地址和端口，注册API路由，挂载静态文件服务，并启动开发者模式的命令行监听线程。
   - `handlers.rs`: **API请求处理器**。包含了所有API端点的具体实现逻辑，例如用户注册、登录、获取地图数据、修改玩家数值等。它是连接前端请求和后端业务逻辑的桥梁。
   - `database.rs`: **数据持久化层**。封装了所有与文件系统交互的操作，如读写玩家存档（`PlayerData`）、用户凭据、手动存档等。它提供了清晰的接口供`handlers.rs`调用，隔离了数据存储的细节。
   - `models.rs`: **关键数据结构定义**。使用Rust的`struct`定义了整个项目用到的所有数据模型，如`PlayerData`, `GameObject`, `PackedMapData`等，并利用`serde`库进行JSON序列化和反序列化。
   - `loader.rs`: **地图数据加载器**。核心的游戏内容处理模块。负责从JSON文件中读取地图和物件的原始数据，并根据当前玩家的状态（`PlayerData`）进行动态处理（如过滤掉不应显示的物件），最后打包成`PackedMapData`格式。
   - `auth.rs`: **认证模块**。负责JWT的生成和验证逻辑。它定义了令牌的载荷（`Claims`）和有效期，并提供了Actix Web的请求守卫（`FromRequest`），可以方便地在Handler中获取已认证的用户信息。
   - `achievements.rs`: **成就系统模块**。负责加载所有成就的定义文件，并提供了核心的`check_and_unlock_achievements`函数，用于根据玩家数值的变化判断是否解锁新成就。
   - `dev_mode.rs`: **开发者模式模块**。实现了一个在服务器控制台运行的命令行工具，允许开发者在游戏运行时动态修改玩家数据，极大地方便了调试。
2. **前端文件结构 (位于 `js/` 目录下):**
   - **根目录:** 存放各个HTML页面专属的主逻辑文件，如`login.js`, `main_menu.js`, `achievement.js`, `save-system.js`。
   - `js/game/`: **核心游戏引擎目录**。
     - `main.js`: 游戏页面的主入口和**游戏循环（Game Loop）**所在地。负责初始化所有游戏模块、加载地图、控制镜头、并在每一帧调用更新函数。
     - `config.js`: 全局配置文件，存放了玩家速度、初始地图、音量等可调整的常量，便于统一管理。
   - `js/game/modules/`: **核心功能模块目录**。
     - `dataManager.js`: **前端数据中心与API客户端**。这是前端最重要的模块之一，它封装了所有与后端API的通信逻辑，并维护一个全局的玩家数据缓存`window.playerDataCache`，为其他模块提供统一的数据获取和修改接口（`gameState.getValue`, `gameState.modifyValue`）。
     - `world/`: **游戏世界相关模块**。
       - `builder.js`: 负责根据后端传来的地图数据动态创建DOM元素（地图背景、物件、墙体等）。
       - `player.js`: 定义了玩家对象，处理键盘输入与玩家移动。
       - `collision.js`: 实现了玩家与墙体的碰撞检测算法。
       - `interaction.js`: 实现了玩家与可交互物件的检测与交互逻辑。
       - `dialogue.js`: 对话系统的状态机，负责加载剧本、管理对话流程。
     - `dialogue_renderer.js`: 对话系统的“视图”层，专门负责将对话状态渲染成具体的HTML页面元素。
     - `audioManager.js`, `achievementNotifier.js`, `debug.js`: 其他辅助模块，分别负责音频、成就通知和调试信息的显示。

------



#### **三、 关键数据结构**



项目的核心逻辑围绕着一系列精心设计的数据结构展开，这些结构在后端`src/models.rs`中定义，并通过JSON与前端进行交互。

1. **`PlayerData` (玩家核心存档):** 这是最重要的数据结构，完整描述了一个玩家的所有状态。
   - `username`: 用户名。
   - `address`: 记录玩家当前的位置（地图ID和坐标），用于游戏加载和自动保存。
   - `values`: 一个哈希表（`HashMap<String, i32>`），存储了所有影响游戏进程的关键变量，如“怀疑度”（`suspicion`）、任务进度等。这是游戏动态性的核心。
   - `map_states`: 记录了每个地图的特定状态，例如哪些一次性物件已被移除，确保玩家再次进入地图时状态能够被还原。
   - `achievements`: 一个字符串向量（`Vec<String>`），存储了玩家已解锁的所有成就的ID。
   - `total_play_time_seconds`, `last_login_timestamp`: 用于精确计算玩家的总游戏时长。
2. **`PackedMapData` (打包地图数据):** 这是后端处理完地图数据后，最终发送给前端的格式。它不是原始数据，而是为客户端“量身定制”的即用型数据包。
   - `name`, `background`, `width`, `height`: 地图的基本信息。
   - `walls`: 碰撞墙体数组。
   - `objects`: **可见物件数组**。这些是根据玩家当前`PlayerData`判断应该立即显示的物件。
   - `latent_objects`: **潜在物件数组**。这些物件的显示条件当前不满足，但后续可能因玩家数值变化而变为可见。前端会持续监控它们。
   - `asset_manifest`: 资源清单，包含了本地图所有需要用到的图片URL。前端会利用这个清单进行**图片预加载**，优化了地图切换时的体验。
   - `music`: 本地图的背景音乐路径。
3. **`GameObject` (游戏物件):** 定义了地图上所有可交互或不可交互的实体。
   - `id`, `x`, `y`, `width`, `height`, `image`: 物件的基本属性。
   - `story_key`: 如果物件可交互，这里会指定交互时触发的剧本ID。
   - `required_values`: **条件显示的核心**。这是一个`RequiredValues`结构体，内部包含一个`Condition`数组。它定义了该物件要变得可见所必须满足的条件（例如玩家的`suspicion`值大于等于50）。
4. **`Condition` / `RequiredValues` (条件判断结构):** 这是实现游戏动态性的基石，不仅用于物件的条件显示，也用于对话选项的条件出现和成就解锁。
   - `name`: 要检查的玩家数值名称（如`suspicion`）。
   - `comparison`: 比较方式（如`GreaterThanOrEqual`）。
   - `value`: 要比较的目标值。
   - `logic`: `AND`或`OR`，定义了多个`Condition`之间的逻辑关系。

------



#### **四、 核心算法与逻辑实现**



1. 动态地图加载与对象过滤算法 (后端 loader.rs + 前端 main.js):

   这是一个典型的**“后端预处理 + 前端动态响应”**的混合算法，兼顾了加载性能和游戏动态性。

   - **后端预处理流程 (`loader.rs::load_and_pack_map_data`):**
     1. 当玩家请求进入某地图时，服务器首先加载该玩家的完整`PlayerData`。
     2. 然后，加载该地图定义文件及其引用的所有`GameObject`文件。
     3. **核心算法:** 遍历所有`GameObject`。对每一个物件，调用`should_display_object`函数，该函数会解析物件的`required_values`字段，并逐一与玩家的`PlayerData`中的`values`进行比较。
     4. 根据比较结果，将物件分别放入`visible_objects`（可见）和`latent_objects`（潜在）两个列表中。
     5. 最后，将这两个列表连同其他地图信息打包成`PackedMapData`发给前端。这个过程将大量的逻辑判断放在了性能更强的服务器端，减轻了客户端的初次加载负担。
   - **前端动态响应流程 (`main.js::checkDynamicObjects`):**
     1. 前端收到`PackedMapData`后，只渲染`objects`列表中的可见物件。`latent_objects`列表则被保存在内存中。
     2. `dataManager.js`中设置了一个回调机制`onValueChange`。每当玩家的数值（`values`）因游戏行为发生改变并与后端同步成功后，这个回调就会被触发。
     3. `checkDynamicObjects`函数作为回调被执行。它会遍历内存中的`latent_objects`列表，用最新的玩家数值重新进行一次`shouldDisplayObject`判断。
     4. 如果有潜在物件的条件现在满足了，就将其从`latent_objects`中移除，动态创建其DOM元素并添加到游戏世界中，同时将其加入可交互列表。
     5. 这个机制使得游戏世界可以对玩家的行为做出实时反馈（例如，完成某个任务后，一个隐藏的宝箱出现），而无需重新加载整个地图。

2. 玩家移动与碰撞检测算法 (前端 collision.js):

   为了实现流畅且精确的碰撞效果，我们采用了分离轴（Axis-Separated）碰撞检测算法。

   1. 游戏循环的每一帧，`player.js`根据当前按键状态计算出玩家的“目标坐标”`targetX`和`targetY`。
   2. `handlePlayerCollision`函数接收玩家对象和所有墙体数据。
   3. **算法核心:** 它不直接检测目标坐标，而是分两步进行：
      - **X轴检测:** 先创建一个只应用了`targetX`（Y坐标保持不变）的假想矩形。遍历所有墙体，检查这个假想矩形是否与任何墙体碰撞。如果碰撞，则根据移动方向将玩家的最终X坐标修正为紧贴墙边的位置（例如，向右移动碰到墙，`finalX = wall.x - player.width`）。
      - **Y轴检测:** 接下来，创建一个应用了**上一步修正后的X坐标**和`targetY`的假想矩形。再次遍历所有墙体进行碰撞检测。如果碰撞，则同理修正最终的Y坐标。
   4. 这种先处理X轴再处理Y轴的方法，可以有效解决当玩家同时斜向撞向墙角时被“卡住”的问题，使得沿墙滑动非常顺滑。

3. 交互系统与智能提示算法 (前端 interaction.js):

   该系统不仅检测交互，还实现了一个体验良好的“智能”提示。

   1. 每一帧，`interactionManager.update`会计算玩家中心点与所有可交互物件中心点的距离。
   2. 它会找到距离最近且在`INTERACTION_RADIUS`（交互半径）内的物件作为`currentInteractable`。
   3. **智能提示定位算法:**
      - 如果检测到可交互物件，`updatePrompt`函数被调用以显示交互提示（"E"键）。
      - 为了避免提示遮挡玩家或物件，它的位置是动态计算的：首先判断玩家相对于物件的主要方向（上下左右）。
      - 例如，如果玩家在物件的右边，提示就会智能地出现在物件的左边；如果玩家在物件的下方，提示就会出现在物件的上方。
      - 同时，算法还会考虑提示框本身的大小和预设的视觉间距`BASE_VISUAL_GAP`，确保定位精准美观。
   4. 当玩家按下'E'键时，系统会根据物件的`storyKey`启动对话管理器。

------



#### **五、 前端交互性实现**



本项目的前端完全由JavaScript驱动，实现了丰富的交互体验。

1. **模块化架构:** 如文件结构所述，整个前端应用被拆分成数十个独立的ES6模块。例如，`player.js`只关心玩家自身的状态和移动，`collision.js`只关心碰撞计算，`dataManager.js`只关心与服务器的数据同步。这种高度解耦的架构使得代码逻辑清晰，易于扩展和维护。
2. **游戏主循环 (`requestAnimationFrame`):** `main.js`中的`gameLoop`函数是整个游戏的“心跳”。它使用`requestAnimationFrame` API来确保动画的流畅性和性能。在每一帧，它会依次执行：玩家输入更新、碰撞检测与位置修正、交互检测、镜头更新等一系列操作，驱动整个游戏世界的动态变化。
3. **事件驱动模型:** 项目广泛使用事件驱动模型来响应用户操作。
   - `keydown`/`keyup`事件被`player.js`监听，用于控制角色移动。
   - `keydown`事件也被`interaction.js`监听，用于触发'E'键交互。
   - 所有UI界面（主菜单、存档、成就）都通过`click`事件与用户交互。
4. **异步数据流管理:** `dataManager.js`是异步处理的典范。它使用`async/await`将复杂的异步`fetch`请求封装成看似同步的、易于理解的函数调用（如`gameState.loadPlayerData()`）。这使得其他模块在需要数据时可以简单地`await`一个函数，而无需处理繁琐的Promise回调，同时保证了在数据加载过程中UI不会被阻塞。地图加载前的**加载屏幕（Loading Screen）**和 **图片预加载** 机制就是这一模式的成功应用。
5. **动态DOM操作:** 游戏世界不是静态的HTML，而是由JavaScript动态构建和管理的。
   - `builder.js`在加载地图时，会根据后端数据循环创建代表物件和墙体的`<div>`元素并添加到DOM中。
   - `dialogue_renderer.js`在对话进行时，会实时更新角色立绘、对话文本和选项按钮。
   - `achievementNotifier.js`在解锁成就时，会动态创建一个通知DOM元素，添加CSS动画，并在几秒后自动移除它。

------



#### **六、 视觉设计与兼容性**



本项目的视觉设计旨在营造一种沉浸式的科幻氛围，并通过现代CSS技术实现动态、美观且具备良好兼容性的用户界面。

1. **视觉风格与设计:** 项目的整体视觉风格统一，围绕“科幻”、“未来科技”和“数据流”等核心概念展开。这具体体现在以下几个方面：
   - **动态背景:** 登录页面（`htmls/login.html`）、主菜单（`htmls/index.html`）和存档系统（`htmls/save-system.html`）等多个关键界面都采用了纯CSS实现的动态粒子背景效果。通过在`<body>`或特定容器内用JS动态创建多个`.particle`元素，并为其应用CSS动画（如`float`动画），我们创造了一个不断浮动、富有深度的星空或数据粒子背景，极大地增强了页面的视觉吸引力。
   - **光影与渐变效果:** 界面中广泛使用了线性渐变（`linear-gradient`）、阴影（`box-shadow`）和发光效果（`text-shadow`, `filter: drop-shadow()`）来模拟科技产品的光感。例如，主菜单的标题使用了渐变背景并裁切为文字形状（`-webkit-background-clip: text`），成就系统的标题和进度条也大量运用了此类技术，营造出霓虹灯或能量条的质感。
   - **玻璃拟态 (Glassmorphism):** 登录框（`htmls/login.html`）、用户信息侧边栏（`htmls/index.html`）等元素采用了`backdrop-filter: blur()`属性，创建了流行的毛玻璃效果，使其在复杂的动态背景下依然保持了良好的信息辨识度。
   - **动画与过渡:** 项目中几乎所有的交互元素都配有平滑的CSS `transition`效果。按钮在鼠标悬停时会轻微上浮和放大（`transform: translateY(-2px) scale(1.05)`），模态框（Modal）出现时则伴随着淡入和上滑的动画，这些细节极大地提升了用户操作的反馈感和流畅度。
2. **CSS响应式设计:** 项目的前端页面充分考虑了在不同尺寸设备上的显示效果，通过CSS媒体查询（Media Queries）实现了响应式布局。
   - **布局自适应:**
     - **Grid布局应用:** “关于我们”页面的成员展示区使用了CSS Grid布局（`display: grid`）。在桌面端，它是一个六列布局；当屏幕宽度小于1100px时，自动变为三列；当小于650px时，则变为两列，确保在任何设备上都能合理利用空间。
     - **Flexbox布局应用:** 存档系统的头部（`.save-header`）在桌面端使用Flexbox实现左右对齐布局，而在小于768px的屏幕上，则通过`flex-direction: column`切换为垂直堆叠布局，避免了内容拥挤。
     - **自适应网格:** 存档卡片列表使用了`grid-template-columns: repeat(auto-fill, minmax(350px, 1fr))`。这是一种非常现代且高效的响应式技术，它允许浏览器根据容器的可用宽度自动决定每行可以容纳多少个卡片，无需硬编码断点。
   - **内容调整:** 在小屏幕下，除了布局变化，还会相应地调整字体大小、内外边距等，以保证最佳的可读性。例如，在`mainMenu.css`中，当屏幕宽度小于768px时，游戏标题的字号和按钮的内边距都有所减小。
3. **浏览器兼容性:** 项目使用了大量现代 `CSS3` 和 `JavaScript ES6` 特性，旨在为现代浏览器用户提供最佳体验。
   - **兼容性基线:** 项目可以良好地兼容所有支持ES6模块、Flexbox、Grid布局、CSS动画以及 `backdrop-filter` 等特性的现代主流浏览器，包括**Chrome、Firefox、Edge和Safari**的近两年版本。
   - **潜在兼容性问题:**
     - **老旧浏览器:** 由于直接使用了ES6的 `import/export` 语法，项目无法在不支持此特性的老旧浏览器（如Internet Explorer 11）上运行。
     - **浏览器前缀:** 代码中部分高级CSS属性（如`-webkit-background-clip`）已包含`webkit`前缀以兼容基于 `webkit` 内核的浏览器，但并未全面覆盖所有需要前缀的属性，在一些特定版本的浏览器上可能存在显示差异。
     - **性能考量:** 大量的动画和 `backdrop-filter` 效果对GPU有一定要求。在性能较差的设备上，可能会出现轻微的卡顿。但在主流设备上，得益于`requestAnimationFrame` 和CSS硬件加速，游戏运行流畅。
   - **未来优化:** 若要支持更广泛的浏览器，可引入Babel进行JS代码降级编译，并使用 `Autoprefixer` 工具自动添加CSS浏览器前缀。

------



#### **七、 总结**



本项目成功地构建了一个前后端分离、功能完善的Web游戏应用。后端采用Rust语言，保证了服务器的稳定性和高性能；前端采用模块化的原生JavaScript，实现了灵活、可扩展的游戏逻辑和流畅的交互体验。

项目的技术亮点包括：

- **数据驱动的动态世界:** 通过`PlayerData`和`RequiredValues`数据结构，实现了游戏物件、对话和成就的条件触发，构建了一个能对玩家行为做出实时反馈的动态游戏世界。
- **高效的混合加载策略:** 结合了后端预处理和前端动态响应的地图加载算法，在保证游戏动态性的同时，优化了客户端的加载性能。
- **清晰的模块化架构:** 前后端代码均遵循“关注点分离”原则，职责划分明确，代码耦合度低，为未来的功能扩展和维护奠定了坚实的基础。