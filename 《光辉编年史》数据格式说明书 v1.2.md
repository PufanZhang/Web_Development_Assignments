### 《光辉编年史》数据格式说明书 v1.2



这份文档是在《光辉编年史》世界中创作冒险、谱写故事的蓝图。它将详细解释如何通过配置简单的 `.json` 文件，来创建地图、放置物品、设计对话和实现复杂的剧情逻辑。

相较于 v1.1 版本，v1.2 新增和优化了以下重要功能，能让故事更加生动和强大：

- **入场剧情**: 现在可以为地图设置一个“入场故事”，当玩家第一次进入地图时，会自动播放一段剧情。
- **一次性互动**: 可以轻松地将某个物品设置为“一次性的”，玩家与它互动一次后，它就会从游戏中消失。
- **更强大的显示逻辑**: 物体的出现条件得到了增强，现在支持多个条件之间的“与”(AND) / “或”(OR) 逻辑，可以设计出更复杂的隐藏要素。

------



#### 一、总览



我们整个游戏的核心都围绕着 `.json` 文件。它们定义了游戏世界的一切：地图长什么样、地图上有什么东西，以及玩家与这些东西交互时会发生什么故事。

主要有三类核心数据：

1. **地图 (`/data/maps`)**：定义一个游戏场景的宏观数据，比如背景、墙体、尺寸和其中包含的物体。
2. **物体 (`/data/objects`)**：定义了可以放置在地图上的具体实体，比如一个道具、一个NPC或是一个传送门。
3. **故事 (`/data/stories`)**：定义了与物体交互时触发的对话、剧情分支和游戏数值变化。

------



#### 二、地图文件 (`/data/maps/*.json`)



地图文件负责“画”出整个游戏场景。它像一个大“舞台”，告诉程序这个场景叫什么、长什么样，以及上面要“摆放”哪些“道具”（物体）。

目录位置: /data/maps/

命名约定: 你的地图名.json (例如: map1.json)

**字段说明：**

| 字段名          | 类型   | 是否必需 | 描述                                                         |
| --------------- | ------ | -------- | ------------------------------------------------------------ |
| `name`          | String | 是       | 地图的名称。比如 "起源之地"。                                |
| `background`    | String | 是       | 指向地图背景图片的路径。例如 `"assets/images/map1.png"`。    |
| `width`         | Number | 否       | **(v1.2新增)**地图的宽度（单位：像素）。默认为 `800`。当地图远大于玩家视口时，需要设置此项以实现镜头滚动。 |
| `height`        | Number | 否       | **(v1.2新增)**地图的高度（单位：像素）。默认为 `600`。       |
| `walls`         | Array  | 否       | 一个包含多个“墙体”对象的数组。用于设置碰撞体积，防止玩家穿墙。 |
| `objects`       | Array  | 否       | 一个**字符串数组**，包含了所有要在这个地图上**初始加载**的物体的 **文件名（不带.json后缀）**。 |
| `entryStoryKey` | String | 否       | **(v1.2 新增)** 玩家进入该地图时自动触发的**故事文件名（不带.json后缀）**。非常适合用于制作开场动画或区域介绍。 |

**`walls` 内部对象结构：**

| 字段名   | 类型   | 描述                |
| -------- | ------ | ------------------- |
| `x`      | Number | 墙体左上角的X坐标。 |
| `y`      | Number | 墙体左上角的Y坐标。 |
| `width`  | Number | 墙体的宽度。        |
| `height` | Number | 墙体的高度。        |

示例 (map1.json):

这个例子展示了一个非常巨大的地图，并且设置了入场剧情。

```
{
  "name": "起源之地",
  "width": 16000,
  "height": 10000,
  "background": "assets/images/map1.png",
  "entryStoryKey": "map1_entry_story",
  "walls": [
    { "x": 700, "y": 400, "width": 100, "height": 100 }
  ],
  "objects": [ "item_in_here01", "portal1to2", "increase_suspicion", "decrease_suspicion", "portal1toF" ]
}
```

------



#### 三、物体文件 (`/data/objects/*.json`)



物体是地图上的核心交互元素。它可以是一个能对话的NPC、一个能捡的道具、一个传送门，甚至是一个满足特定条件才会出现的隐藏物品。

目录位置: /data/objects/

命名约定: 你的物体名.json (例如: portal1to2.json)

**字段说明：**

| 字段名                      | 类型    | 是否必需 | 描述                                                         |
| --------------------------- | ------- | -------- | ------------------------------------------------------------ |
| `id`                        | String  | 是       | 物体的唯一标识符，**必须和文件名保持一致**。例如 `"portal1to2"`。 |
| `image`                     | String  | 是       | 指向该物体图片的路径。例如 `"assets/images/portal.png"`。    |
| `x`, `y`, `width`, `height` | Number  | 是       | 物体在地图上的位置和尺寸。                                   |
| `storyKey`                  | String  | 是       | 关联的**故事文件名（不带.json后缀）**。当玩家与此物体交互时，会加载并开始这个故事。 |
| `showPrompt`                | Boolean | 否       | 是否在玩家靠近时显示交互提示（'E'键）。默认为 `false`。适合做成隐藏或被动触发的物体。 |
| `singleInteraction`         | Boolean | 否       | **(v1.2 新增)** 如果设为 `true`，玩家与该物体交互并**完成关联故事后**，该物体将**永久消失**。默认为 `false`。非常适合用于一次性拾取的道具或只触发一次的事件。 |
| `teleportData`              | Object  | 否       | 如果这个物体是传送门，就需要这个字段。                       |
| `requiredValues`            | Object  | 否       | **(v1.2 增强)** 物体**出现**的条件。如果玩家的数值不满足这些条件，这个物体根本不会出现在地图上。 |

**`teleportData` 对象结构：**

| 字段名               | 类型   | 描述                                    |
| -------------------- | ------ | --------------------------------------- |
| `targetMap`          | String | 目标地图的**文件名（不带.json后缀）**。 |
| `targetX`, `targetY` | Number | 玩家传送到目标地图后的坐标。            |

**`requiredValues` 对象结构 (v1.2 增强):**

| 字段名       | 类型   | 描述                                                         |
| ------------ | ------ | ------------------------------------------------------------ |
| `logic`      | String | **(v1.2 新增)** 条件的组合逻辑。`"AND"` 表示所有条件都必须满足；`"OR"` 表示只需满足任意一个条件。默认为 `"AND"`。 |
| `conditions` | Array  | **(v1.2 新增)** 一个包含所有具体条件的数组。                 |

**`conditions` 数组内的对象结构:**

| 字段名       | 类型   | 描述                                                         |
| ------------ | ------ | ------------------------------------------------------------ |
| `name`       | String | 需要检查的玩家数值的名称，例如 `"suspicion"`。               |
| `comparison` | String | 比较方式。**注意：** 此处的值必须是以下几种之一：`"greater_than"`, `"less_than"`, `"equal"`, `"greater_than_or_equal"`, `"less_than_or_equal"`, `"not_equal"`。 |
| `value`      | Number | 用于比较的阈值。                                             |

示例 (high_suspicion_item.json):

这个物品只有当玩家的 "suspicion" (怀疑度) 大于等于200时才会出现。

```
{
  "id": "high_suspicion_item",
  "image": "assets/images/item_03.png",
  "x": 400, "y": 150, "width": 60, "height": 60,
  "storyKey": "high_suspicion_story",
  "showPrompt": true,
  "requiredValues": {
    "logic": "AND",
    "conditions": [
      { "name": "suspicion", "comparison": "greater_than_or_equal", "value": 200 }
    ]
  }
}
```

------



#### 四、故事文件 (`/data/stories/*.json`)



故事是整个游戏最有趣的部分！它是一个由多个“节点”组成的对话树，通过对话、选项和数值变化，来驱动剧情发展。

目录位置: /data/stories/

命名约定: 你的故事名.json (例如: portal_story.json)

**字段说明：**

| 字段名      | 类型   | 是否必需 | 描述                                                         |
| ----------- | ------ | -------- | ------------------------------------------------------------ |
| `startNode` | String | 是       | 故事开始的节点ID。例如 `"confirm"`。                         |
| `nodes`     | Object | 是       | 包含所有故事节点的“字典”，`key` 是节点ID，`value` 是节点对象。 |

**`nodes` 内部的节点对象结构：**

| 字段名         | 类型   | 是否必需 | 描述                                                         |
| -------------- | ------ | -------- | ------------------------------------------------------------ |
| `speaker`      | String | 否       | 对话者的名字。如果是 `"旁白"`，则不显示名字。                |
| `dialogue`     | String | 是       | 当前节点显示的对话文本。                                     |
| `nextNode`     | String | 否       | 如果该节点是线性对话（没有选项），此字段指向下一个节点的ID。 |
| `options`      | Array  | 否       | 如果该节点提供选项，则此字段为一个选项对象数组。**`nextNode` 和 `options` 通常不同时存在。** |
| `action`       | String | 否       | 在节点结束时触发的特殊动作。目前有 `"teleport"` (触发传送，需配合物体上的`teleportData`) 和 `"minigameFTG"` (触发战斗小游戏)。 |
| `valueChanges` | Array  | 否       | 一个包含数值变化对象的数组，用于改变玩家的属性。             |
| `scene`        | Array  | 否       | 定义当前场景需要显示哪些角色立绘。                           |

**`options` 内部的选项对象结构：**

| 字段名          | 类型   | 是否必需 | 描述                                                         |
| --------------- | ------ | -------- | ------------------------------------------------------------ |
| `text`          | String | 是       | 选项按钮上显示的文字。                                       |
| `targetNode`    | String | 是       | 点击该选项后要跳转到的节点ID。                               |
| `requiredValue` | Object | 否       | 控制**选项是否显示**。结构和物体里的类似，但有一个**重要区别**见下文。 |

options 中 requiredValue 的对象结构:

这里的 comparison 值与物体出现条件完全相同。

| 字段名       | 类型   | 描述                                                         |
| ------------ | ------ | ------------------------------------------------------------ |
| `name`       | String | 需要检查的玩家数值的名称，例如 `"suspicion"`。               |
| `comparison` | String | 比较方式。此处的值必须是以下几种之一：`"greater_than"`, `"less_than"`, `"equal"`, `"greater_than_or_equal"`, `"less_than_or_equal"`, `"not_equal"`。 |
| `value`      | Number | 用于比较的阈值。                                             |

**`valueChanges` 内部的数值变化对象结构：**

| 字段名   | 类型   | 描述                                                         |
| -------- | ------ | ------------------------------------------------------------ |
| `name`   | String | 要改变的数值名称，例如 `"suspicion"`。                       |
| `amount` | Number | 改变的量。正数表示增加，负数表示减少。例如 `100` 或 `-100`。 |

**`scene` 内部的角色对象结构：**

| 字段名   | 类型   | 描述                            |
| -------- | ------ | ------------------------------- |
| `id`     | String | 角色的唯一ID，与`speaker`对应。 |
| `sprite` | String | 指向该角色立绘图片的路径。      |

示例 (map2_secret_story.json):

这个故事展示了如何根据玩家的 suspicion (怀疑度) 来显示或隐藏一个特定的对话选项。

```
{
  "startNode": "start",
  "nodes": {
    "start": {
      "speaker": "旁白",
      "dialogue": "你面前的这个东西散发着微光，似乎隐藏着什么秘密。",
      "options": [
        {
          "text": "“我已经洞悉了真相！”",
          "targetNode": "secret_node",
          "requiredValue": {
            "name": "suspicion",
            "comparison": "greaterOrEqual",
            "value": 100
          }
        },
        { "text": "“只是个普通的发光体而已。”", "targetNode": "normal_end" }
      ]
    },
    "secret_node": {
      "speaker": "？？？",
      "dialogue": "你终于来了……"
    },
    "normal_end": {
      "speaker": "旁白",
      "dialogue": "你没有发现任何特别之处。"
    }
  }
}
```
